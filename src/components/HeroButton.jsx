import React from "react";
import "./HeroButton.css"; // This is where the CSS will be imported

const HeroButton = ({ children }) => {
  return <button className="button">Lets dive in</button>;
};

export default HeroButton;
